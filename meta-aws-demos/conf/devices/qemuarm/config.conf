MACHINE = "qemuarm"

# not many arm64 dev notebooks are used yet
SDKMACHINE = "x86_64"

# oe-core's qemu.inc defaults to compressed ext4.zst, which runqemu can't boot
# without "snapshot". Add an uncompressed ext4 and default runqemu to it.
IMAGE_FSTYPES += "ext4"
QB_DEFAULT_FSTYPE = "ext4"
