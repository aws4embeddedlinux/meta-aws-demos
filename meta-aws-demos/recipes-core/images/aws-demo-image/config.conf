# Included into local.conf for aws-demo-image (see conf/templates/default/local.conf.sample).

# This image installs several consumers of the AWS CRT stack, so link one shared
# standalone stack (see the shared include for the full rationale).
require ${OEROOT}/../meta-aws-demos/conf/include/aws-crt-shared-stack.inc
